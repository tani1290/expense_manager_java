services:
  expense-manager:
    build: .
    container_name: expense-manager
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - expense-data:/data
    environment:
      - TZ=Asia/Kolkata

volumes:
  expense-data:
